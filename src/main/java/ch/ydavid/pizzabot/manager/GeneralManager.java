package ch.ydavid.pizzabot.manager;

public class GeneralManager {

    private DynamicVoiceManager dynamicVoiceManager = new DynamicVoiceManager();

    public DynamicVoiceManager getDynamicVoiceManager() {
        return dynamicVoiceManager;
    }
}
